<% @devices.each do |device| 
  if device.checkins.count > 0 then
    time = device.checkins.last.uptime
    minutes = time/60
    seconds = time - (minutes * 60)
    hours = minutes / 60
    minutes-=hours * 60
    days = hours / 24
    hours-=days * 24
  %>
    <%= device.name %> => 
    <%= device.checkins.last.uptime %> 
    (<%=days%>:<%=hours%>:<%=minutes%>:<%= seconds %>)
    @ <%= device.checkins.last.created_at %>, 
    <%= device.checkins.count %> total heartbeats.
  <% else %>
    <%= device.name %> => has not checked in.
  <% end %>
<br />
<% end %>
<hr />
<b>This is uptime data for a handful of servers.  To add your server to this list, simply create a cron job that visits http://robertcarpenter.net/uptime/&lt;computer-name&gt;/[&lt;passkey&gt;/]checkin/&lt;seconds-alive&gt;[/&lt;checkin-method&gt;]</b>
